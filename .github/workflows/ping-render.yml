name: Keep Render Awake

on:
  schedule:
    - cron: "*/5 * * * *" 
  workflow_dispatch: 

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render backend
        run: |
          curl -s --max-time 10 \
          curl -s https://api2.swiv.xyz/health || echo "Ping failed"
